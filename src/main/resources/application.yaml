spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/db_my_wallet
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: wallet123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: none

